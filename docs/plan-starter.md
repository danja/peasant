# Peasant

This will be an LLM coding harness CLI written in Node.js along the same lines as OpenCode, Claude Code and Codex with two key differences : it will only use processor instructions supported by the Athlon II (OpenCode etc crash on this); it will address free APIs such as the free tier of Groq and Mistral.